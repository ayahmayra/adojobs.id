:8080 {
    root * /app/public
    
    # PHP FrankenPHP without worker mode for local development
    php_server
    
    # Basic logging for development
    log {
        output stdout
        level INFO
    }
}
